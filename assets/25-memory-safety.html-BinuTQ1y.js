import{_ as s}from"./plugin-vue_export-helper-DlAUqK2U.js";import{c as a,b as e,o as t}from"./app-BOuFtv26.js";const p={};function o(c,n){return t(),a("div",null,n[0]||(n[0]=[e(`<p>动态内存的语法并不难，难的是维护一组始终成立的约束：访问不能越界、对象必须仍然存活、资源只能释放一次，并且最终必须有人释放。</p><h2 id="先建立-合法访问-的四个条件" tabindex="-1"><a class="header-anchor" href="#先建立-合法访问-的四个条件"><span>先建立&quot;合法访问&quot;的四个条件</span></a></h2><p>通过指针访问对象之前，至少确认：</p><ol><li>指针不是空指针；</li><li>指针指向的对象仍在生命周期内；</li><li>访问范围没有越过对象边界；</li><li>访问方式与对象类型、对齐要求相容。</li></ol><p>只检查 <code>p != NULL</code> 远远不够。<span style="color:#F56C6C;">非空地址也可能悬空、越界或类型错误。</span></p><h2 id="未初始化读取" tabindex="-1"><a class="header-anchor" href="#未初始化读取"><span>未初始化读取</span></a></h2><div class="language-c line-numbers-mode" data-highlighter="prismjs" data-ext="c"><pre><code class="language-c"><span class="line"><span class="token keyword">int</span> value<span class="token punctuation">;</span></span>
<span class="line"><span class="token function">printf</span><span class="token punctuation">(</span><span class="token string">&quot;%d\\n&quot;</span><span class="token punctuation">,</span> value<span class="token punctuation">)</span><span class="token punctuation">;</span>  <span class="token comment">// 未定义行为</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>自动对象不会自动变成零。编译器可能把未初始化读取当作&quot;不可能发生&quot;，从而做出令初学者意外的优化。最稳妥的习惯是让对象在第一次读取前拥有明确值。</p><p><strong>危害与如何发现：</strong></p><ul><li><strong>危害</strong>：读取到的值是内存中残留的随机数据。这个值可能看起来正常（比如碰巧是 0），让程序继续执行并产生错误的结果；也可能是一个很大的负数或正数，导致后续运算溢出、数组访问越界或逻辑判断错误。最麻烦的是程序在 Debug 模式下每次运行结果一致，Release 模式下却行为诡异——因为编译器对未初始化操作的优化不同。</li><li><strong>如何发现</strong>：用 <code>-fsanitize=undefined</code> 编译，检测到未初始化读取时会报告。开启 <code>-Wall</code> 编译器会对某些未初始化变量给出警告。用调试器逐步跟踪并观察变量值是否有反常的大数也能辅助定位。</li></ul><h2 id="数组越界" tabindex="-1"><a class="header-anchor" href="#数组越界"><span>数组越界</span></a></h2><div class="language-c line-numbers-mode" data-highlighter="prismjs" data-ext="c"><pre><code class="language-c"><span class="line"><span class="token keyword">int</span> data<span class="token punctuation">[</span><span class="token number">4</span><span class="token punctuation">]</span> <span class="token operator">=</span> <span class="token punctuation">{</span><span class="token number">0</span><span class="token punctuation">}</span><span class="token punctuation">;</span></span>
<span class="line">data<span class="token punctuation">[</span><span class="token number">4</span><span class="token punctuation">]</span> <span class="token operator">=</span> <span class="token number">10</span><span class="token punctuation">;</span>  <span class="token comment">// 合法下标只有 0、1、2、3</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>C 通常不会自动检查边界。写出的地址可能覆盖另一个局部变量、返回信息或尚未分配的页面。<span style="color:#E6A23C;">程序&quot;这次没有崩溃&quot;不等于代码正确。</span></p><p>统一使用半开区间可以减少边界错误：长度为 <code>n</code> 的数组，其合法范围是 <code>[0, n)</code>。</p><p><strong>危害与如何发现：</strong></p><ul><li><strong>危害</strong>：覆盖相邻内存中的其他变量会导致数据静默损坏——你以为是正确的结果，其实已经是篡改过的值。如果越界写入覆盖了函数的返回地址（参见第 18 篇的缓冲区溢出章节），攻击者可以利用它执行任意代码。越界写入还可能导致后续的 <code>free</code> 操作崩溃，因为堆管理信息被破坏了。</li><li><strong>如何发现</strong>：用 <code>-fsanitize=address</code> 编译，程序越界访问时会立即报错并打印堆栈信息，精确指明是哪一行、访问了什么地址。Valgrind 也能检测堆上的越界。对比 Debug 和 Release 版本的不同行为也是一个线索。</li></ul><h2 id="缓冲区溢出" tabindex="-1"><a class="header-anchor" href="#缓冲区溢出"><span>缓冲区溢出</span></a></h2><div class="language-c line-numbers-mode" data-highlighter="prismjs" data-ext="c"><pre><code class="language-c"><span class="line"><span class="token keyword">char</span> name<span class="token punctuation">[</span><span class="token number">8</span><span class="token punctuation">]</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token function">scanf</span><span class="token punctuation">(</span><span class="token string">&quot;%s&quot;</span><span class="token punctuation">,</span> name<span class="token punctuation">)</span><span class="token punctuation">;</span>  <span class="token comment">// 输入过长会越界</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>至少要限制宽度：</p><div class="language-c line-numbers-mode" data-highlighter="prismjs" data-ext="c"><pre><code class="language-c"><span class="line"><span class="token function">scanf</span><span class="token punctuation">(</span><span class="token string">&quot;%7s&quot;</span><span class="token punctuation">,</span> name<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><span style="color:#67C23A;">但更推荐用 <code>fgets</code> 读取一整行，再解析：</span></p><div class="language-c line-numbers-mode" data-highlighter="prismjs" data-ext="c"><pre><code class="language-c"><span class="line"><span class="token keyword">char</span> line<span class="token punctuation">[</span><span class="token number">64</span><span class="token punctuation">]</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token function">fgets</span><span class="token punctuation">(</span>line<span class="token punctuation">,</span> <span class="token keyword">sizeof</span> line<span class="token punctuation">,</span> <span class="token constant">stdin</span><span class="token punctuation">)</span> <span class="token operator">==</span> <span class="token constant">NULL</span><span class="token punctuation">)</span> <span class="token punctuation">{</span></span>
<span class="line">    <span class="token comment">/* 处理 EOF 或读取错误 */</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>fgets</code> 可能保留换行符；如果一行长于缓冲区，还要识别并丢弃剩余部分。</p><p><strong>危害与如何发现：</strong></p><ul><li><strong>危害</strong>：缓冲区溢出是最经典的网络安全漏洞之一。攻击者可以精心构造超长输入，覆盖栈上的返回地址，让程序跳转到攻击者指定的代码（shellcode），从而获得系统的控制权。历史上 Morrus 蠕虫、SQL Slammer 等著名攻击都利用了缓冲区溢出。即使在非网络环境下，溢出也会破坏相邻数据，导致程序崩溃或产生错误结果。</li><li><strong>如何发现</strong>：<code>-fsanitize=address</code> 能检测栈上的缓冲区溢出。使用 <code>fgets</code> 或指定宽度的 <code>scanf</code> 可以从源头上避免问题。如果程序在特定输入下崩溃，尝试输入超长字符串看是否触发，也可以作为排查方向。</li></ul><h2 id="使用已释放内存" tabindex="-1"><a class="header-anchor" href="#使用已释放内存"><span>使用已释放内存</span></a></h2><div class="language-c line-numbers-mode" data-highlighter="prismjs" data-ext="c"><pre><code class="language-c"><span class="line"><span class="token keyword">int</span> <span class="token operator">*</span>p <span class="token operator">=</span> <span class="token function">malloc</span><span class="token punctuation">(</span><span class="token keyword">sizeof</span> <span class="token operator">*</span>p<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token keyword">if</span> <span class="token punctuation">(</span>p <span class="token operator">==</span> <span class="token constant">NULL</span><span class="token punctuation">)</span> <span class="token keyword">return</span> <span class="token number">1</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token function">free</span><span class="token punctuation">(</span>p<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token function">printf</span><span class="token punctuation">(</span><span class="token string">&quot;%d\\n&quot;</span><span class="token punctuation">,</span> <span class="token operator">*</span>p<span class="token punctuation">)</span><span class="token punctuation">;</span>  <span class="token comment">// use-after-free</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>free</code> 不会把所有指针副本自动改成 <code>NULL</code>，也不保证立即清空内容。释放后，分配器可以把同一块存储交给别的对象。</p><p>把当前变量设成 <code>NULL</code> 有助于阻止它被再次使用：</p><div class="language-c line-numbers-mode" data-highlighter="prismjs" data-ext="c"><pre><code class="language-c"><span class="line"><span class="token function">free</span><span class="token punctuation">(</span>p<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">p <span class="token operator">=</span> <span class="token constant">NULL</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>但如果还有 <code>alias = p</code> 之类的别名，它们仍然悬空。真正的解决方案是明确所有权，避免到处保存可释放资源的裸别名。</p><p><strong>危害与如何发现：</strong></p><ul><li><strong>危害</strong>：释放后的内存可能已经被分配给其他变量，读取会得到错误的数据；写入则会破坏其他正常对象，导致难以追溯的 bug。最危险的是，如果释放后分配器把这块内存交给了某个安全敏感的对象（如密码缓冲区），旧的指针还能读到它，造成信息泄露。</li><li><strong>如何发现</strong>：<code>-fsanitize=address</code> 对 use-after-free 有极高的检测精度，几乎能 100% 捕获。Valgrind 也能检测。如果程序在看似无关的代码位置崩溃，或者在 <code>malloc</code>/<code>free</code> 操作附近出问题，应怀疑有悬空指针。</li></ul><h2 id="重复释放" tabindex="-1"><a class="header-anchor" href="#重复释放"><span>重复释放</span></a></h2><div class="language-c line-numbers-mode" data-highlighter="prismjs" data-ext="c"><pre><code class="language-c"><span class="line"><span class="token function">free</span><span class="token punctuation">(</span>p<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token function">free</span><span class="token punctuation">(</span>p<span class="token punctuation">)</span><span class="token punctuation">;</span>  <span class="token comment">// 未定义行为</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p><code>free(NULL)</code> 是安全的，但释放同一个非空分配两次不是。一个资源应有一个明确拥有者，拥有者负责恰好释放一次。</p><p><strong>危害与如何发现：</strong></p><ul><li><strong>危害</strong>：重复释放会破坏堆内存管理器的内部状态。第二次 <code>free</code> 时，分配器会尝试操作一块已经不包含有效管理信息的区域，导致程序立即崩溃（有时是段错误，有时是 <code>free(): double free detected in tcache</code> 这样的信息）。更糟糕的是，某些环境下它不会立即崩溃，而是悄声破坏堆结构，等到后续的 <code>malloc</code> 或 <code>free</code> 时才崩，让你查不清根因。</li><li><strong>如何发现</strong>：运行时会收到 <code>free(): double free detected</code> 或类似的消息（glibc 会主动检测并报错）。<code>-fsanitize=address</code> 也能检测。释放指针后立即将其置为 <code>NULL</code>，可以避免同一个变量被二次释放。</li></ul><h2 id="内存泄漏" tabindex="-1"><a class="header-anchor" href="#内存泄漏"><span>内存泄漏</span></a></h2><div class="language-c line-numbers-mode" data-highlighter="prismjs" data-ext="c"><pre><code class="language-c"><span class="line"><span class="token keyword">void</span> <span class="token function">leak</span><span class="token punctuation">(</span><span class="token keyword">void</span><span class="token punctuation">)</span> <span class="token punctuation">{</span></span>
<span class="line">    <span class="token keyword">int</span> <span class="token operator">*</span>data <span class="token operator">=</span> <span class="token function">malloc</span><span class="token punctuation">(</span><span class="token number">100</span> <span class="token operator">*</span> <span class="token keyword">sizeof</span> <span class="token operator">*</span>data<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">    <span class="token keyword">if</span> <span class="token punctuation">(</span>data <span class="token operator">==</span> <span class="token constant">NULL</span><span class="token punctuation">)</span> <span class="token keyword">return</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line">    <span class="token comment">/* 忘记 free(data) */</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><span style="color:#409EFF;">函数返回后，指针变量消失了，但那块动态存储仍然被占用，并且程序已经失去地址。</span>短命令行工具结束时系统会回收进程资源，但长时间运行的服务、循环调用或库代码会不断累积。</p><p><strong>危害与如何发现：</strong></p><ul><li><strong>危害</strong>：短小程序问题不大——进程退出时操作系统会收回所有内存。但对于服务器、游戏引擎、嵌入式设备等需要长时间运行的程序，泄漏会持续消耗内存，最终导致：内存耗尽、系统 OOM（Out of Memory）Killer 杀掉进程、频繁的交换分区读写（性能雪崩）、程序最终被操作系统强制终止。</li><li><strong>如何发现</strong>：用 <code>-fsanitize=address</code> 编译，程序退出时会打印泄漏报告，包含泄漏的大小和分配时的调用栈。Valgrind 的 <code>--leak-check=full</code> 也能做同样的事。运行时观察进程内存占用（Windows 任务管理器、Linux <code>top</code>/<code>htop</code>、<code>ps aux</code> 中的 RSS 列）持续上涨而不回落，也是典型泄漏信号。</li></ul><h2 id="realloc-的正确失败路径" tabindex="-1"><a class="header-anchor" href="#realloc-的正确失败路径"><span><code>realloc</code> 的正确失败路径</span></a></h2><div class="language-c line-numbers-mode" data-highlighter="prismjs" data-ext="c"><pre><code class="language-c"><span class="line"><span class="token keyword">int</span> <span class="token operator">*</span>new_data <span class="token operator">=</span> <span class="token function">realloc</span><span class="token punctuation">(</span>data<span class="token punctuation">,</span> new_count <span class="token operator">*</span> <span class="token keyword">sizeof</span> <span class="token operator">*</span>data<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token keyword">if</span> <span class="token punctuation">(</span>new_data <span class="token operator">==</span> <span class="token constant">NULL</span><span class="token punctuation">)</span> <span class="token punctuation">{</span></span>
<span class="line">    <span class="token comment">// 原来的 data 仍然有效，不能丢掉</span></span>
<span class="line">    <span class="token function">free</span><span class="token punctuation">(</span>data<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">    <span class="token keyword">return</span> <span class="token number">1</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line">data <span class="token operator">=</span> new_data<span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>不要直接写：</p><div class="language-c line-numbers-mode" data-highlighter="prismjs" data-ext="c"><pre><code class="language-c"><span class="line">data <span class="token operator">=</span> <span class="token function">realloc</span><span class="token punctuation">(</span>data<span class="token punctuation">,</span> new_size<span class="token punctuation">)</span><span class="token punctuation">;</span>  <span class="token comment">// 失败时会覆盖唯一地址，造成泄漏</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>当请求大小为零时，不同标准版本和实现细节容易让代码难以阅读。想释放就直接 <code>free</code>，不要把 <code>realloc(ptr, 0)</code> 当作通用释放方式。</p><h2 id="分配大小也会溢出" tabindex="-1"><a class="header-anchor" href="#分配大小也会溢出"><span>分配大小也会溢出</span></a></h2><div class="language-c line-numbers-mode" data-highlighter="prismjs" data-ext="c"><pre><code class="language-c"><span class="line"><span class="token class-name">size_t</span> bytes <span class="token operator">=</span> count <span class="token operator">*</span> <span class="token keyword">sizeof</span><span class="token punctuation">(</span><span class="token keyword">int</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>如果 <code>count</code> 太大，乘法可能按 <code>size_t</code> 回绕，得到一个很小的结果。随后循环仍按原 <code>count</code> 写入，就会严重越界。</p><div class="language-c line-numbers-mode" data-highlighter="prismjs" data-ext="c"><pre><code class="language-c"><span class="line"><span class="token macro property"><span class="token directive-hash">#</span><span class="token directive keyword">include</span> <span class="token string">&lt;stdint.h&gt;</span></span></span>
<span class="line"></span>
<span class="line"><span class="token keyword">if</span> <span class="token punctuation">(</span>count <span class="token operator">&gt;</span> SIZE_MAX <span class="token operator">/</span> <span class="token keyword">sizeof</span><span class="token punctuation">(</span><span class="token keyword">int</span><span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token punctuation">{</span></span>
<span class="line">    <span class="token keyword">return</span> <span class="token number">1</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"><span class="token keyword">int</span> <span class="token operator">*</span>data <span class="token operator">=</span> <span class="token function">malloc</span><span class="token punctuation">(</span>count <span class="token operator">*</span> <span class="token keyword">sizeof</span> <span class="token operator">*</span>data<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>SIZE_MAX</code> 是 <code>size_t</code> 能表示的最大值，常由 <code>&lt;stdint.h&gt;</code> 提供。如果希望不依赖该宏，也可以直接用 <code>(size_t)-1</code> 得到同一类型的最大值：</p><div class="language-c line-numbers-mode" data-highlighter="prismjs" data-ext="c"><pre><code class="language-c"><span class="line"><span class="token keyword">if</span> <span class="token punctuation">(</span>count <span class="token operator">!=</span> <span class="token number">0</span> <span class="token operator">&amp;&amp;</span> <span class="token keyword">sizeof</span> <span class="token operator">*</span>data <span class="token operator">&gt;</span> <span class="token punctuation">(</span><span class="token class-name">size_t</span><span class="token punctuation">)</span><span class="token operator">-</span><span class="token number">1</span> <span class="token operator">/</span> count<span class="token punctuation">)</span> <span class="token punctuation">{</span></span>
<span class="line">    <span class="token keyword">return</span> <span class="token number">1</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>危害与如何发现：</strong></p><ul><li><strong>危害</strong>：乘法溢出导致申请到比预期小得多的内存，后续按原 <code>count</code> 循环写入会造成严重的堆溢出，破坏其他对象或堆管理信息。这种漏洞同样可以被攻击者利用。</li><li><strong>如何发现</strong>：<code>-fsanitize=address</code> 会捕获后续的越界写入。<code>-fsanitize=undefined</code> 可以检测 <code>size_t</code> 乘法溢出。在分配前做溢出检查是更主动的防御。</li></ul><h2 id="返回局部变量地址" tabindex="-1"><a class="header-anchor" href="#返回局部变量地址"><span>返回局部变量地址</span></a></h2><div class="language-c line-numbers-mode" data-highlighter="prismjs" data-ext="c"><pre><code class="language-c"><span class="line"><span class="token keyword">int</span> <span class="token operator">*</span><span class="token function">bad</span><span class="token punctuation">(</span><span class="token keyword">void</span><span class="token punctuation">)</span> <span class="token punctuation">{</span></span>
<span class="line">    <span class="token keyword">int</span> value <span class="token operator">=</span> <span class="token number">42</span><span class="token punctuation">;</span></span>
<span class="line">    <span class="token keyword">return</span> <span class="token operator">&amp;</span>value<span class="token punctuation">;</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>函数返回后 <code>value</code> 的生命周期结束。这个错误与 <code>free</code> 后使用本质相同：指针还保存着一个数值，但对象已经不存在。</p><p><strong>危害与如何发现：</strong></p><ul><li><strong>危害</strong>：拿到的指针指向的是一块已经被回收的栈空间。在调用者读取之前，这块内存可能被后续的函数调用覆盖（包括 <code>printf</code> 或其他库函数），产生随机值。更糟糕的是，如果你通过这个指针写入数据，会破坏调用链上的其他栈帧，导致程序在完全无关的位置崩溃。</li><li><strong>如何发现</strong>：大多数编译器会对返回局部变量地址给出警告（开启 <code>-Wall</code>）。如果你在 Debug 版本中看到函数返回的指针能正确工作，但在 Release 版本中失败，这就是典型嫌疑。</li></ul><h2 id="未定义行为为什么危险" tabindex="-1"><a class="header-anchor" href="#未定义行为为什么危险"><span>未定义行为为什么危险</span></a></h2><p>未定义行为不只是&quot;结果随机&quot;。编译器可以假定正确程序不会触发它，并据此删除分支、重排访问或推导出更强结论。因此：</p><ul><li>Debug 版本正常、Release 版本崩溃并不奇怪；</li><li>加一行打印后错误消失，不表示修好了；</li><li>换台机器结果不同，也不表示某台机器&quot;兼容&quot;。</li></ul><h2 id="用工具尽早发现错误" tabindex="-1"><a class="header-anchor" href="#用工具尽早发现错误"><span>用工具尽早发现错误</span></a></h2><p>GCC 或 Clang 的学习阶段命令：</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code class="language-bash"><span class="line">gcc demo.c <span class="token parameter variable">-std</span><span class="token operator">=</span>c11 <span class="token parameter variable">-Wall</span> <span class="token parameter variable">-Wextra</span> <span class="token parameter variable">-Wpedantic</span> <span class="token punctuation">\\</span></span>
<span class="line">    <span class="token parameter variable">-fsanitize</span><span class="token operator">=</span>address,undefined -fno-omit-frame-pointer <span class="token parameter variable">-g</span> <span class="token parameter variable">-o</span> demo</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><ul><li>AddressSanitizer 常用于发现越界、释放后使用和重复释放，程序退出时还会报告内存泄漏。</li><li>UndefinedBehaviorSanitizer 可发现许多整数溢出、对齐和非法操作问题。</li><li>警告和 Sanitizer 不能证明程序完全安全，但能显著缩短定位时间。</li></ul><h2 id="所有权表" tabindex="-1"><a class="header-anchor" href="#所有权表"><span>所有权表</span></a></h2><p>对动态资源写代码前，先列清楚：</p><table><thead><tr><th>问题</th><th>示例答案</th></tr></thead><tbody><tr><td>谁创建？</td><td><code>list_create</code></td></tr><tr><td>谁拥有？</td><td>返回值接收者</td></tr><tr><td>谁可以借用？</td><td>遍历函数，只在调用期间</td></tr><tr><td>谁释放？</td><td><code>list_destroy</code></td></tr><tr><td>释放后怎样表示？</td><td>拥有者指针设为 <code>NULL</code></td></tr></tbody></table><p>只要所有权无法用一句话说清楚，接口就值得重新设计。</p><p>内存中的数据只在当前进程生命期内有效。下一篇学习文件输入输出，让程序能够保存结果、读取配置，并处理来自外部世界的不可信数据。</p>`,73)]))}const r=s(p,[["render",o]]),d=JSON.parse(`{"path":"/posts/C-Cpp-%E8%AF%AD%E8%A8%80%E4%B8%8E%E7%A8%8B%E5%BA%8F%E8%AE%BE%E8%AE%A1/25-memory-safety.html","title":"内存安全：越界、泄漏与悬空指针","lang":"zh-CN","frontmatter":{"title":"内存安全：越界、泄漏与悬空指针","date":"2026-07-06T00:00:00.000Z","icon":"shield-halved","order":25,"category":["C/C++ 语言与程序设计"],"tag":["C","内存安全","未定义行为","Sanitizer"],"author":"Kingcq","description":"动态内存的语法并不难，难的是维护一组始终成立的约束：访问不能越界、对象必须仍然存活、资源只能释放一次，并且最终必须有人释放。 先建立\\"合法访问\\"的四个条件 通过指针访问对象之前，至少确认： 指针不是空指针； 指针指向的对象仍在生命周期内； 访问范围没有越过对象边界； 访问方式与对象类型、对齐要求相容。 只检查 p != NULL 远远不够。非空地址也可...","head":[["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"Article\\",\\"headline\\":\\"内存安全：越界、泄漏与悬空指针\\",\\"image\\":[\\"\\"],\\"datePublished\\":\\"2026-07-06T00:00:00.000Z\\",\\"dateModified\\":\\"2026-07-24T15:06:06.000Z\\",\\"author\\":[{\\"@type\\":\\"Person\\",\\"name\\":\\"Kingcq\\"}]}"],["meta",{"property":"og:url","content":"https://kingcxp.github.io/posts/C-Cpp-%E8%AF%AD%E8%A8%80%E4%B8%8E%E7%A8%8B%E5%BA%8F%E8%AE%BE%E8%AE%A1/25-memory-safety.html"}],["meta",{"property":"og:site_name","content":"Kingcq's Blog"}],["meta",{"property":"og:title","content":"内存安全：越界、泄漏与悬空指针"}],["meta",{"property":"og:description","content":"动态内存的语法并不难，难的是维护一组始终成立的约束：访问不能越界、对象必须仍然存活、资源只能释放一次，并且最终必须有人释放。 先建立\\"合法访问\\"的四个条件 通过指针访问对象之前，至少确认： 指针不是空指针； 指针指向的对象仍在生命周期内； 访问范围没有越过对象边界； 访问方式与对象类型、对齐要求相容。 只检查 p != NULL 远远不够。非空地址也可..."}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:locale","content":"zh-CN"}],["meta",{"property":"og:updated_time","content":"2026-07-24T15:06:06.000Z"}],["meta",{"property":"article:author","content":"Kingcq"}],["meta",{"property":"article:tag","content":"Sanitizer"}],["meta",{"property":"article:tag","content":"未定义行为"}],["meta",{"property":"article:tag","content":"内存安全"}],["meta",{"property":"article:tag","content":"C"}],["meta",{"property":"article:published_time","content":"2026-07-06T00:00:00.000Z"}],["meta",{"property":"article:modified_time","content":"2026-07-24T15:06:06.000Z"}]]},"git":{"createdTime":1784565366000,"updatedTime":1784905566000,"contributors":[{"name":"Kingcq","username":"Kingcq","email":"404291187@qq.com","commits":3,"url":"https://github.com/Kingcq"}]},"readingTime":{"minutes":9.76,"words":2929},"filePathRelative":"posts/C-Cpp-语言与程序设计/25-memory-safety.md","excerpt":"<p>动态内存的语法并不难，难的是维护一组始终成立的约束：访问不能越界、对象必须仍然存活、资源只能释放一次，并且最终必须有人释放。</p>\\n<h2>先建立\\"合法访问\\"的四个条件</h2>\\n<p>通过指针访问对象之前，至少确认：</p>\\n<ol>\\n<li>指针不是空指针；</li>\\n<li>指针指向的对象仍在生命周期内；</li>\\n<li>访问范围没有越过对象边界；</li>\\n<li>访问方式与对象类型、对齐要求相容。</li>\\n</ol>\\n<p>只检查 <code>p != NULL</code> 远远不够。<span style=\\"color: #F56C6C;\\">非空地址也可能悬空、越界或类型错误。</span></p>","autoDesc":true}`);export{r as comp,d as data};
